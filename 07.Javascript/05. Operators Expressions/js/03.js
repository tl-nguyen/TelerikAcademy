(function () {
	'use strict';
	var width = 17,
		height = 5,
		area = width * height;
	
	myConsole.appendToConsole('Task 03:', ['Width = ' + width + ', Height = ' + height + ' => Area = ' + area]);
}());