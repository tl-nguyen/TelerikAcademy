using System;

class HexFormatAssign
{
    static void Main()
    {
        int hexFormatInteger = 0xFE;

        Console.WriteLine("the hexadecimal format of 0x{0:X} is {1} in decimal format", hexFormatInteger, hexFormatInteger);
    }
}
